---
title: "HSMR Summary Report - {{publication_period}}"
author: "Automated HSMR Pipeline"
date: "{{generation_date}}"
output:
  html_document:
    toc: no
    theme: united
# params:
#   publication_period: "YYYY QN"
#   generation_date: "YYYY-MM-DD"
---

```{r setup_summary, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE)
# publication_period <- ifelse(exists("params") && !is.null(params$publication_period), params$publication_period, "TEST YYYY QN")
# generation_date <- ifelse(exists("params") && !is.null(params$generation_date), params$generation_date, Sys.Date())
```

# HSMR Key Findings: `r ifelse(exists("params") && !is.null(params$publication_period), params$publication_period, "YYYY QN")`

This summary provides key highlights from the HSMR report.
Generated on: `r ifelse(exists("params") && !is.null(params$generation_date), params$generation_date, Sys.Date())`.

**Overall SMR (Placeholder):** 1.01
*(This would be dynamically loaded from data in a real report)*

**Key Trend (Placeholder):** Stable mortality trend observed.
*(This would be dynamically derived).*

---
*For detailed information, please see the Full HSMR Main Report.*
*This summary was generated automatically.*
---
